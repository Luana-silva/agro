import { DateFormatPipe3 } from './date-format.pipe';

describe('DateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatPipe3();
    expect(pipe).toBeTruthy();
  });
});
